# Protocol - Guild ID: {{ meta.guild_id }}

**Period:** {{ meta.start_time.strftime('%Y-%m-%d %H:%M:%S') }} to {{ meta.end_time.strftime('%Y-%m-%d %H:%M:%S') }}

## Participants
{% for user in meta.users.values() -%}
- {{ user.name }} (ID: {{ user.id }}, Channel: {{ user.channel }})
{% endfor %}

## History
{% for item in timeline -%}
{% if item.type == 'event' -%}
- `[{{ item.timestamp.strftime('%H:%M:%S') }}]` *{{ item.content }}*
{% else -%}
> **{{ item.user_name }}** `[{{ item.timestamp.strftime('%H:%M:%S') }}]`: {{ item.text }}
{% endif %}
{% endfor %}
